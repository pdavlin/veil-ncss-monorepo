{
  "name": "@veil-ncss/site-ncss",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NCSS site — Eleventy site consuming tokens-base + tokens-ncss + components.",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve --port=8081",
    "clean": "rm -rf _site",
    "typecheck": "echo 'no typecheck — Eleventy site'",
    "lint": "echo 'lint runs at monorepo root'",
    "test": "echo 'no unit tests — covered by build + a11y check'"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-img": "^5.0.0",
    "nunjucks": "^3.2.4",
    "@veil-ncss/components": "workspace:*",
    "@veil-ncss/tokens-base": "workspace:*",
    "@veil-ncss/tokens-ncss": "workspace:*"
  }
}
