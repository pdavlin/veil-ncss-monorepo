{
  "name": "@veil-ncss/tokens-shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./contrast": "./src/contrast.ts",
    "./utopia": "./src/utopia.ts",
    "./emit-css": "./src/emit-css.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "build": "echo 'no build step (consumed via source)'",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "culori": "^4.0.1",
    "utopia-core": "^1.2.0"
  },
  "devDependencies": {
    "@types/culori": "^2.1.1"
  }
}
